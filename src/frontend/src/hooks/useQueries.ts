// This file is intentionally minimal as the app doesn't require backend queries
// All animation and interaction logic is handled client-side

export function useQueries() {
  // No backend queries needed for this animation app
  return {};
}
